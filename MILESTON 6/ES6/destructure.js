const hena = {
    name : 'hena',
    age : 10,
    friends : ['hero alom','bappa'],
    stutus : 'not found',
}

const {name,age} = hena;

console.log(age);

const person = ['hero alom',{name: 'gias'},'hena','bapparaj'];

const [nayok,obj] = person;
console.log(obj)

const [text,objj,namee,sadNayok] = person;
console.log(text,sadNayok);


